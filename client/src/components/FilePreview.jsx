import { fileUrl } from '../api/client';
export function FilePreview({ attachment }){ if(!attachment) return <span className="text-sm text-slate-500">Sin archivo</span>; const url=fileUrl(attachment.url); return <div className="rounded-xl border p-3"><p className="mb-2 text-sm font-semibold">{attachment.originalName}</p>{attachment.mimeType?.startsWith('image/') ? <img src={url} className="max-h-56 rounded-lg object-contain"/> : <iframe src={url} className="h-56 w-full rounded-lg" title={attachment.originalName}/>}<a className="mt-2 inline-block text-sm font-semibold text-blue-700" href={url} target="_blank">Abrir archivo</a></div>; }
