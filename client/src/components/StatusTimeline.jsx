import { labels } from './StatusBadge';
export function StatusTimeline({ history=[] }){ if(!history.length) return <p className="text-sm text-slate-500">Sin movimientos registrados.</p>; return <ol className="relative border-l border-slate-200 pl-4">{history.map(h=><li key={h.id} className="mb-5"><span className="absolute -left-1.5 mt-1 h-3 w-3 rounded-full bg-blue-700"/><p className="font-semibold">{labels[h.status] || h.status}</p><p className="text-sm text-slate-600">{h.comment || 'Sin observaciones'}</p><p className="text-xs text-slate-400">{new Date(h.changedAt).toLocaleString('es-AR')} · {h.changedBy?.name}</p></li>)}</ol>; }
