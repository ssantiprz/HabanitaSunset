import { Link, NavLink, Outlet } from 'react-router-dom';
import { ClipboardList, Home, LogOut, PlusCircle, Shield, Truck, Users } from 'lucide-react';
import { useAuth } from '../context/AuthContext';
const item='flex items-center gap-3 rounded-xl px-3 py-2 text-sm font-medium';
export function Layout(){ const { user, logout, isAdmin, isRequester }=useAuth(); return <div className="min-h-screen lg:flex"><aside className="bg-slate-950 text-white lg:w-72"><div className="flex items-center justify-between p-4 lg:block"><Link to="/" className="text-lg font-bold">Materiales Municipio</Link><button className="lg:hidden" onClick={logout}><LogOut size={18}/></button></div><nav className="flex gap-2 overflow-x-auto px-4 pb-4 lg:block lg:space-y-1"><NavLink to="/" className={({isActive})=>`${item} ${isActive?'bg-blue-700':'hover:bg-slate-800'}`}><Home size={18}/>Inicio</NavLink><NavLink to="/solicitudes" className={({isActive})=>`${item} ${isActive?'bg-blue-700':'hover:bg-slate-800'}`}><ClipboardList size={18}/>Solicitudes</NavLink>{isRequester && <NavLink to="/solicitudes/nueva" className={({isActive})=>`${item} ${isActive?'bg-blue-700':'hover:bg-slate-800'}`}><PlusCircle size={18}/>Nueva</NavLink>} {isAdmin && <NavLink to="/admin" className={({isActive})=>`${item} ${isActive?'bg-blue-700':'hover:bg-slate-800'}`}><Shield size={18}/>Admin</NavLink>}</nav><div className="hidden border-t border-slate-800 p-4 lg:block"><p className="text-sm font-semibold">{user?.name}</p><p className="text-xs text-slate-400">{user?.role}</p><button onClick={logout} className="mt-3 flex items-center gap-2 text-sm text-slate-300"><LogOut size={16}/>Cerrar sesión</button></div></aside><main className="flex-1"><header className="border-b bg-white px-4 py-3"><div className="flex items-center justify-between"><div><p className="text-xs uppercase tracking-wide text-slate-500">Plataforma operativa</p><h1 className="text-xl font-bold">Gestión de Solicitudes</h1></div><div className="hidden items-center gap-2 rounded-full bg-blue-50 px-3 py-2 text-sm text-blue-900 sm:flex"><Truck size={16}/>{user?.supplier?.name || 'Municipio'}</div></div></header><section className="p-4 lg:p-6"><Outlet/></section></main></div>; }
