import { useState } from 'react'; import { Navigate, useNavigate } from 'react-router-dom'; import { useAuth } from '../context/AuthContext'; import { useToast } from '../components/Toast';
export function LoginPage(){ const { user, login }=useAuth(); const [email,setEmail]=useState('admin@demo.com'); const [password,setPassword]=useState('admin123'); const [loading,setLoading]=useState(false); const nav=useNavigate(); const toast=useToast(); if(user) return <Navigate to="/"/>; const submit=async(e)=>{ e.preventDefault(); setLoading(true); try{ await login(email,password); toast('Bienvenido/a'); nav('/'); }catch(err){ toast(err.message,'error'); }finally{ setLoading(false); } }; return <main className="grid min-h-screen place-items-center bg-gradient-to-br from-blue-950 via-slate-900 to-blue-800 p-4"><form onSubmit={submit} className="w-full max-w-md rounded-3xl bg-white p-8 shadow-2xl"><p className="text-sm font-bold uppercase text-blue-700">Acceso municipal</p><h1 className="mt-2 text-3xl font-black">Gestión de Materiales</h1><p className="mt-2 text-slate-600">Ingresá para administrar solicitudes, facturas, remitos y entregas.</p><label className="mt-6 block text-sm font-semibold">Email</label><input className="input mt-1" value={email} onChange={e=>setEmail(e.target.value)}/><label className="mt-4 block text-sm font-semibold">Contraseña</label><input className="input mt-1" type="password" value={password} onChange={e=>setPassword(e.target.value)}/><button disabled={loading} className="btn-primary mt-6 w-full">{loading?'Ingresando...':'Iniciar sesión'}</button><div className="mt-6 rounded-2xl bg-slate-50 p-4 text-xs text-slate-600"><b>Demo:</b><br/>admin@demo.com / admin123<br/>solicitante@demo.com / demo123<br/>proveedor@demo.com / demo123</div></form></main>; }
